plugins {
    id 'buildlogic.maven-central-bom-conventions'
    id 'buildlogic.sonatype-conventions'
}

description = 'BOM'

javaPlatform {
    allowDependencies()
}

dependencies {
    constraints {
        api libs.bc.bom
        api libs.java.jwt

        rootProject.findProject('projects-core').subprojects { subproject ->
            if (subproject.name != 'bayonet-bom') {
                api(subproject)
            }
        }
    }
}
